 # Linux文件系统管理
>相关概念：
对于一个文件系统，以Linux的EXT2文件系统为例，对其进行格式化就是根据没中操作系统设置的文件属性/权限。
在内部实际分成了两种不同的区块：
- inode：和文件是一一对应的，一个文件对应一个inode，里面存储的是文件的元信息，为文件的属性，同时记录实际存放文件数据的block号码；
- block：实际存放文件内容的块，是文件系统进行分配的最小单位，所以每个block只能存放一个文件的内容，但一个文件可能占多个block，所以block设置太大可能使得小文件浪费很多内存，但是设置过小又会使一个文件占用过多block，给inode对这些block的记录占用过多空间。
- superblock：记录这个filesystem的整体信息。

通过读取block的方式分为索引式和FAT格式
1. 索引式，一次读取所有的block位置，EXT2系统采用
2. FAT格式，每次读取一个block，链式存储，读完一个才能知道下一个。

Block会分组，分为若干个blockgroup，每个group里面独立管理inode/block/superblock

每个inode节点会记录12个直接地址，一个间接地址，一个二级间接地址，一个三级间接地址。
一个间接地址对应的是一个全是记录实际地址的block，依次类推；
所以对于一个1kB的block，一个block记录要4Byte，一个就可以记录256个Block，对于个三级地址就是256*256*256*1kB=2^34B=16GB
所以如果block扩大两倍，理论上一个inode对应的这个文件上限会提高为2^3(三级block)*2（block本身）=16倍

一个磁盘可以划分成多个partition，就是对应一个起止的扇区，一个partition上可以创建一个独立的文件系统（需要真正按照该文件系统格式进行格式化）
一个格式化的文件系统对应一系列block group
一个block group里面包含block group0，block group1...block groupn
其中只有一个真正的superblock是整个文件系统的描述信息，一般放在group0中，其余的group如果有则是备份数据用于异常恢复。
以下group0的结构为例子：
首先无论是什么结构，如inode，block还是superblock，其最小存储单位都是一个个block
信息包括：
- 首先superblock的位置：即其block编号
- 文件系统描述说明的位置：即其block编号
- block bitmap和inode bitmap：即他们的block编号位置
- inode table位置：如161-672号block
那么可以进行如下计算共有多少inode（即对应理论能存储的文件数量最大值）
一个inode记录占256B
一个block占4KB
共有672-161+1=512个block用于存储inode
所以最多可以存 512*4KB/256B=8k=2^13=8192个inode
- group0目前可用的block数量，可用的inode数量，假设可用8181，那么就是已经使用了11个，可用的inode号码就是12-8192号

里面还包括block bitmap和inode bitmap，这个是一个group一个分别进行记录的
其中每一位代表一个block或inode是否已经使用
一般block的数量被设计为2^15个，即一个group所有存储数据的block的最大空间为2^15*4kB=128MB 设计为这么多数量因为刚好为4KB位，用一个bitmap可以正好表示，而inode数量一般为8192，所以一个block大小的bitmap会有富裕。

另外对于一个目录文件，他的具体存储内容的block存储的是目录里面的文件名和这个文件对应的inode等，相当于存储的是目录中的项信息，而文件的block则是存储的真正的文件内容。

superblock存储的是整个文件系统的元信息，包括全部的inode，block情况等，在每个group内部又有自己的Group Descriptor Table，这个是存储的自己group内部的inode和block元信息。
>整体结构如下：
>一块磁盘 Disk
│
├── Partition 1
│   └── ext2 文件系统
│       │
│       ├── Block Group 0
│       │   ├── Superblock
│       │   ├── Group Descriptor Table
│       │   ├── Block Bitmap
│       │   ├── Inode Bitmap
│       │   ├── Inode Table
│       │   └── Data Blocks
│       │
│       ├── Block Group 1
│       │   ├── [Superblock Backup，可能有]
│       │   ├── [GDT Backup，可能有]
│       │   ├── Block Bitmap
│       │   ├── Inode Bitmap
│       │   ├── Inode Table
│       │   └── Data Blocks
│       │
│       ├── Block Group 2
│       │   └── ...
│       │
│       └── Block Group N
│
└── Partition 2
    └── 另一个文件系统

1. 对于目录文件
其inode存储的是目录权限信息和这个目录文件具体分配的block号
而其block存储的并非是实际具体的文件内容，而是该目录下存储的文件名和其对应的inode号。
1. 对于实际文件
inode存储的内容与目录文件相同，而block中实际存储具体的文件内容本身。

metadata和数据存放区域：
metadata：
- Superblock
- Block Bitmap
- Inode Bitmap
区域实际数据存放区域：
inode table，data block

上述两者可能存在数据不一致状态，例如metadata已更改，而文件实际写入的时候出了问题，因此需要检查其一致性，目前的文件系统主要通过日志文件系统来实现。
1. 先在日志记录区域记录某个文件准备写入的信息
2. 写入文件权限和数据
3. 更新meta数据
4. 完成meta更新后，在日志记录块中完成该文件的记录

**挂载的概念 MOUNT**
首先所如之前的内容不变，所谓挂载就是将独立的若干个文件系统挂载到目录树这个逻辑入口上。
其中独立的文件系统对应的是真正底层存储并已经被格式化的块设备（如磁盘分区等），而整个系统的目录树在挂载时就是一个逻辑的挂载点（路径），这个挂载点变成了这个文件系统的根路径，而存储的内容不会改变。
所谓/dev/sda1就是这些块设备的表示形式，他们形似文件，但并不是真正的文件目录等，无法cd，就是表示成这种形式，作为一个逻辑的访问方式。

>举一个例子：
假设系统现在本身的结构是：根目录已经挂载在sda1上了，这些内容实际就是按这个结构存储在sda1里面的，这里因为挂载点就是/，刚好相当sda1文件系统的根路径就对应的是目录树的根路径。（挂载就是让被挂载的文件系统的根路径对应到挂载点的位置去，比如你挂的是/a，那么以后访问/a/111.txt的时候，就是去访问这个文件系统实际存储的/111.txt）
/
  - /data1
    - a.txt
    - b.txt
  - /data2
    - c.txt
然后我有一个设备是/dev/sda2，里面目前已经存储东西了文件系统结构式/d.txt
那么假设我现在执行了mount /dev/sda2 /data2
那么这个/data2这个目录树中的路径就对应到了sda2文件系统的根路径，当你访问这个路径的下的文件中，到data2这里就进入到了sda2的文件系统，所以再执行ls /data2 结果显示的就是d.txt,而之前的c.txt就看不到了，但他实际还是原样存储在sda1的文件系统中，只是现在看不到了。
所以可见挂载设备的内容会被迁移到挂载点处，如果这个挂载点下面本身有数据，那么这个数据就看不到了，但是存储空间依旧被占用，这是非常不好的情况。

再举个例子：假设同时执行
- mount /dev/sda2 /data2
- mount /dev/sda2 /data1
那么这俩东西都变成了这个文件系统根路径的入口了，访问的时候里面都是d.txt了，就变成了完全相同的东西。

这里和windows的区别就是。windows相当于是在入口将不同的文件系统进行区分，展示成不同的盘符形式如E://,D://等，而linux则允许把不同的文件系统挂载在目录树的任何位置。