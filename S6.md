> ls 
>选项与参数：
>-a  ：全部的文件，连同隐藏文件（ 开头为 . 的文件） 一起列出来（常用）
-A  ：全部的文件，连同隐藏文件，但不包括 . 与 .. 这两个目录
-d  ：仅列出目录本身，而不是列出目录内的文件数据（常用）
-f  ：直接列出结果，而不进行排序 （ls 默认会以文件名排序！）
-F  ：根据文件、目录等信息，给予附加数据结构，例如：
      *:代表可可执行文件； /:代表目录； =:代表 socket 文件； &#124;:代表 FIFO 文件；
-h  ：将文件大小以人类较易读的方式（例如 GB, KB 等等）列出来；
-i  ：列出 inode 号码，inode 的意义下一章将会介绍；
-l  ：长数据串行出，包含文件的属性与权限等等数据；（常用）
-n  ：列出 UID 与 GID[…]”

> cp
-d 如果是link文件的话就复制link而不是文件本身
-i 如果目标文件已经存在时，在覆盖时会先询问
-p 连同文件的属性一起复制
-s 复制成软连接的形式
-r 递归复制
-l 复制为硬连接的形式
-u 只有destination比source新才更新
-a 相当于有p，d，r

> rm
-f : 忽略不存在的文件
-i : 互动模式
-r ：递归删除

>mv
-u 只有更新的才移动更新
-i 存在相同的时候先询问是否覆盖
-f 直接覆盖

>basename/dirname 分别取得文件完整路径和目录路径

>查看文件
  > `cat`
 -b 显示行号，不显示空白行  
 -n 显示行号，显示空白行
 -T 显示tab
 -E 显示结尾符 $
 -v 显示特殊符号
 -A 相当于vET，就是基本上把特殊符号都收显示出来

  >'tac'
  与cat相同就是从最后一行到第一行反向显示

  >`nl` 默认带行号去显示文件内容
  -b a 和cat -n相同
  -b t 和cat -b相同
  -n ln 行号在屏幕的最左方显示
  -w 数量 行号有几位

  >`more` 按页去展示文件内容
  more的运行过程中，使用这些去进行控制
  回车翻下一行，空白键翻下一页 
  /+string 向下搜索这个string 按n继续搜索
  :f 立即显示出文件名以及目前显示的行数
  q 立即离开more
  b 向前往回翻页

  >`less` 和more相同，对向前翻页更加支持
  less的运行过程中，使用如下控制
  pagedown/up 向下上翻页
  /+string和more相同
  ？+string 向前搜索string
  N可反向搜索
  g 前进到第一行去
  G 到最后一行去
  q 退出

  >`head/tail` 显示对应行数的内容
   -n 数字 显示前面/后面 对应数字行
      -数字/+数字 包括后面xx行/不包括前面xx行
   -f 持续侦测文件内容，知道ctl+c退出

>时间相关
  - mtime：默认显示的，是文件内容最近修改的时间
  - ctime（status time）：是文件状态发生变化的时间
  - atime（access time）：最近发生过读取的时间
  查询的时候可以使用    `ll --time=atime/ctime(默认显示的mtime)` 
  > 使用touch进行修改
    touch -a 修改access时间
    touch -c 修改status时间
    touch -t 修改为想要的时间 格式是YYYYMMDDHHMM
    touch -m 修改mtime

>`umask`用于设置一个用户创建文件和目录的默认权限，后面的三个数字是减去的权限，例如022依次代表对，owner，group和others去掉无，写，写
其中rwx分别是4，2，1，默认情况下创建文件的权限是666，目录是777，所以umask可以设置为022，即去掉非owner的写。对于root则要去掉的内容更多。

>隐藏属性
`chattr` +A 不会更改access时间
         +S 同步写入
         +a 只能增加不能修改删除
         +c 自动压缩
         +s 删除直接完全删除
         +i 不能有任何变动，包括增删改
`lsattr` -a 显示隐藏的文件属性
         -d 如果是目录显示目录的属性
         -R 连同子目录一起显示