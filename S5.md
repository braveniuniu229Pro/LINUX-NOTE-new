# S4 文件系统
20260819
文件权限的三个概念
- 文件拥有者
- 同一群组
- 其他人
三个相关的文件
- /etc/passwd 保存系统账号与一般身份使用者
- /etc/group 个人密码
- /etc/shadow 群组名称

> 相关代码
  `ls -al #查看相关的文件权限`

相关修改权限的命令：
>`chgrp :修改文件所属群组 chgrp [-R] grpname dirname/fiename` 
 `chown :修改文件拥有者 chown [-R] usrname dirname/fiename` 也可同时：chown [-R] usrname:grpname dirname/fiename
 `chmod :修改文件的权限`

>拷贝命令：
`cp 来源文件 目的文件`
`chmod [-r] xyz 文件或目录` xyz依次是三个不同的角色权限：拥有者，同群组，其他人
`chmod [-r] u=rwx go=rx 文件或目录`#ugoa 拥有者，同群组，其他人，所有人

直接对相应角色添加或减少权限
`chmod [-r] 角色+权限（例如a+w） 文件或目录`
对于目录文件来讲：
1. r代表可以看到目录的文件列表，只代表可以看到文件名字，如果没有x的话则无法看到文件的具体信息，而有x没有r则可以，只是无法知道文件的具体名字需要自己补全
2. w代表可以创建删除目录中的文件
3.  x代表可以cd到当前目录路径作为工作路径

>文件种类
>  - 正规文件
    - 纯文本文件（ASCII）
    - 二进制文档
    - 数据格式文件
>  - 目录文件
>  - 链接文件
>  - 设备与设备文件
    - 区块设备文件：用于我随机存取的存储设备 属性为b
    - 字符设备文件：序列埠的周边设备 一次性读取 属性为c
>  - 数据接口文件 socket 属性为c
>  - 数据输送档 FIFO，PIPE，解决多个程序同时存取一个文件造成的错误问题，属性为p

/ 根目录
/usr（unix software resource） ：与软件安装执行相关
/var （variable） ：与系统运行过程有关

FHS定义的根目录下应该有的次目录：
1. /bin 放可执行文件的目录 如car chmod chown date mv mkdir cp bash等
2. /boot 放置开机会使用的文件
3. /dev 放置设备文件 /dev/null /dev/zero /dev/tty
4. /etc 系统主要的配置文件
5. /lib 放置函数库
6. /media 放置可移除设备
7. /mnt 挂载设备
8. /opt 第三方软件放置的目录
9. /run 开机后产生的各项信息
10. /sbin 开机过程所需要的重要指令
11. /srv 服务数据
12. /tmp 暂时放置文件
13. /home 系统默认使用者的主文件夹
14. /proc 所有的数据的都在内存中，保存系统核心和进程信息
15. /sys 记录已载入的核心模块与核心检测到的硬件设备信息

几种不同的路径：
1. ～ 当前用户的主目录 如root就是/root，其他用户就是/home/具体用户
2. - 代表上一个路径
3. ./当前路径
4. ../上一层路径
5. /x/y/z 代表绝对路径

>涉及的常用命令：
`pwd [-P]` 显示当前工作路径，如果加了参数P则如果是链接就显示实际的路径
`mkdir [-mp] 目录名称` 参数m：-m 711 直接设置权限；参数p递归创建文件夹
`rmdir [-p] 目录名称` 参数p同理，递归删除目录文件
 $PATH是系统的环境变量，不同身份对于的默认PATH是不同的，对于任意一条命令会从这些路径中去遍历搜索，匹配到的第一个会被执行，例如ls，第一个PATH是/bin 那么执行的就是/bin/ls

 > ls 
>选项与参数：
>-a  ：全部的文件，连同隐藏文件（ 开头为 . 的文件） 一起列出来（常用）
-A  ：全部的文件，连同隐藏文件，但不包括 . 与 .. 这两个目录
-d  ：仅列出目录本身，而不是列出目录内的文件数据（常用）
-f  ：直接列出结果，而不进行排序 （ls 默认会以文件名排序！）
-F  ：根据文件、目录等信息，给予附加数据结构，例如：
      *:代表可可执行文件； /:代表目录； =:代表 socket 文件； &#124;:代表 FIFO 文件；
-h  ：将文件大小以人类较易读的方式（例如 GB, KB 等等）列出来；
-i  ：列出 inode 号码，inode 的意义下一章将会介绍；
-l  ：长数据串行出，包含文件的属性与权限等等数据；（常用）
-n  ：列出 UID 与 GID[…]”

> cp
-d 如果是link文件的话就复制link而不是文件本身
-i 如果目标文件已经存在时，在覆盖时会先询问
-p 连同文件的属性一起复制
-s 复制成软连接的形式
-r 递归复制
-l 复制为硬连接的形式
-u 只有destination比source新才更新
-a 相当于有p，d，r